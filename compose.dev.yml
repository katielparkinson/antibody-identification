services:
  app:
    image: oven/bun:1
    working_dir: /app
    command: sh -c "bun install && bun run dev --hostname 0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - next_dev:/app/.next-dev
      - bun_cache:/root/.bun
    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
      NEXT_TELEMETRY_DISABLED: "1"

volumes:
  node_modules:
  next_dev:
  bun_cache:
